function setup() {
    video = createCapture(VIDEO);
    video.size(550, 500);

    canvas = createCanvas(1300, 800);
    canvas.position(560, 100);}
    function draw() {
        background('#EC5800');
    }